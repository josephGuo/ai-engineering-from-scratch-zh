# 分级测验答案键

只读取学习者刚完成的那一轮。后续轮次在其答案到来前不要放入上下文。只有五轮全部完成后才揭示解释。

## 第 1 轮：数学与统计

- Q1：A。点积为 `1*4 + 2*5 + 3*6 = 32`。
- Q2：D。恰有两个正面的概率是 `C(3,2) * (1/2)^3 = 3/8`。

## 第 2 轮：经典机器学习

- Q3：B。模型正确预测了 90% 的负样本，却漏掉了 10% 的正样本，所以准确率为 90%。
- Q4：C。树的数量在训练前配置；划分阈值、叶节点预测和节点不纯度都在拟合过程中学习或计算。

## 第 3 轮：深度学习

- Q5：A。链式法则传播导数，因此能计算损失相对于每个权重的梯度。
- Q6：D。残差路径为梯度提供了穿过深层网络的更短路径，缓解由梯度消失造成的退化。

## 第 4 轮：NLP 与 Transformer

- Q7：C。注意力将 queries 与 keys 比较，再用得到的权重组合 values。
- Q8：B。LoRA 冻结基础模型，并训练小型低秩更新矩阵，从而降低可训练参数数量。

## 第 5 轮：应用 AI

- Q9：A。RAG 流水线会先检索相关材料，再将其作为上下文提供给语言模型生成答案。
- Q10：B。协调者会拆解工作、分派任务、路由消息，并管理专用 agent 之间的协作。
